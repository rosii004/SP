Марта и нејзините 3 другарки сакаат да одат на кино и купуваат билети.
Иницијалната цена на еден билет за сите филмови е 200 денари, но 
доколку филмот е комедија се доплаќа 20 денари,
доколку филмот е акција се доплаќа 40 денари
доколку филмот е романса се доплаќа 30 денари
Освен картите кои сакаат да ги купат тие сакаат да купат и нешто за пиење и грицкање. Менито е следното:

Пијалоци:                                                                       Пуканки:
Вода: 80ден                                                                    големина S: 100ден
Fanta, CocaCola, Sprite: 100ден                                     големина M: 150ден
IceTea: 120ден.                                                               големина L: 200ден.

Да се пресмета која ќе биде вкупната цена што Марта и нејзините другарки ќе треба да ја платат.
Дополнително доколку тие одлучат да одат во Среда и платат со нивната VISA картичка за вредноста од 2 билети ќе добијат 4 (односно добиваат 50% попуст на купените билети без разлика за кој филм станува збор)

Објаснување за input 
вид на филм:  action
големина на пуканки:  L
број на пакувања пуканки:  2
вид на пијалок:  Fanta
број на пијалоци:  3
Кој ден ќе гледа филм:  Wednesday
VISA картичка:  1 (ДА) или 0 (НЕ)

#include <iostream>
#include <string>
using namespace std;

int main() {
    string typeFilm, popcornSize, drinkType, day;
    int popcornCount, drinkCount;
    bool visa;

    cin >> typeFilm;
    cin >> popcornSize;
    cin >> popcornCount;
    cin >> drinkType;
    cin >> drinkCount;
    cin >> day;
    cin >> visa;

    int ticketPrice = 200;
    if (typeFilm == "comedy" || typeFilm == "Comedy") ticketPrice += 20;
    else if (typeFilm == "action" || typeFilm == "Action") ticketPrice += 40;
    else if (typeFilm == "romance" || typeFilm == "Romance") ticketPrice += 30;

    int totalTickets = ticketPrice * 4;

    if ((day == "wednesday" || day == "Wednesday") && visa) {
        totalTickets *= 0.5;
    }

    int drinkPrice = 0;
    if (drinkType == "water" || drinkType == "Water") drinkPrice = 80;
    else if (drinkType == "fanta" || drinkType == "Fanta" || drinkType == "cocacola" || drinkType == "CocaCola" || drinkType == "sprite" || drinkType == "Sprite") drinkPrice = 100;
    else if (drinkType == "icetea" || drinkType == "IceTea") drinkPrice = 120;

    int totalDrinks = drinkPrice * drinkCount;

    int popcornPrice = 0;
    if (popcornSize == "s" || popcornSize == "S") popcornPrice = 100;
    else if (popcornSize == "m" || popcornSize == "M") popcornPrice = 150;
    else if (popcornSize == "l" || popcornSize == "L") popcornPrice = 200;

    int totalPopcorn = popcornPrice * popcornCount;

    int total = totalTickets + totalDrinks + totalPopcorn;

    cout << total << endl;

    return 0;
}
