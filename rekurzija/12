#include <iostream>
using namespace std;
int countDigits(int n){
    if (n >= 0 && n <=9)
        return n;

    return (n % 10) + countDigits(n / 10);
}

int main() {
    int n;
    cin>>n;
    cout<<countDigits(n);

    return 0;
}
