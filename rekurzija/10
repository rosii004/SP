#include <iostream>
using namespace std;
void reverseString(string s, int i){
    if (i < 0) return;
    cout<<s[i];

    reverseString(s, i - 1);
}

int main() {
    string str;
    cin>>str;

    reverseString(str, str.length() - 1);
    return 0;
}
