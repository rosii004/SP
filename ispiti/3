Rekurzivna funkcija
Vnesi broj n. Potoa da se pecati niza 
n, n-3, n-6, n-9... n-9, n-6, n-3
Koga ke dojde do negativen broj da ne se odzima do 3 tuku da se soberi so 3 vo sekoj nareden clen

#include <iostream>
using namespace std;

void printSequence(int n, int original) {
    cout << n << " ";
    if (n <= 0) {
        return;
    }
    printSequence(n - 3, original);
    if (n - 3 < 0) {
        int next = n - 3 + 6;
        if (next <= original) cout << next << " ";
    } else {
        int next = n - 3 + 3;
        if (next <= original) cout << next << " ";
    }
}

int main() {
    int n;
    cin >> n;
    void recur(int, int);
    
    recur(n, n);
    return 0;
}
