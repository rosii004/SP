Rekurzivna funkcija
Vnesi broj n. Potoa da se pecati niza 
n, n-3, n-6, n-9... n-9, n-6, n-3
Koga ke dojde do negativen broj da ne se odzima do 3 tuku da se soberi so 3 vo sekoj nareden clen

#include <iostream>
using namespace std;

void printSequence(int n, int original, bool goingDown) {
    cout << n << " ";

    if (n <= 0) {
        goingDown = false;
    }

    if (n == original && !goingDown) {
        return;
    }

    if (goingDown) {
        printSequence(n - 3, original, true);
    } else {
        printSequence(n + 3, original, false);
    }
}

int main() {
    int n;
    cin >> n;

    printSequence(n, n, true);

    return 0;
}
