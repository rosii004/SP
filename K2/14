Да се напише програма во која се вчитува матрица од цели броеви A со N редици и N * 2 колони (N не е поголемо од 50). Програмата треба да ја трансформира вчитаната матрица во нова матрица B, 
така што сите елементи десно од N-тата колона ќе ги префрли под N-тата редица, односно од матрица со 
димензија N редици и 2 * N колони ќе се добие матрица со 2 * N редици x N колони.

#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;
    int A[50][100], B[100][50];

    for (int i = 0; i < N; i++)
        for (int j = 0; j < 2 * N; j++)
            cin >> A[i][j];

    for (int i = 0; i < N; i++)
        for (int j = 0; j < N; j++)
            B[i][j] = A[i][j];

    for (int i = 0; i < N; i++)
        for (int j = 0; j < N; j++)
            B[i + N][j] = A[i][j + N];

    for (int i = 0; i < 2 * N; i++) {
        for (int j = 0; j < N; j++) {
            cout << B[i][j] << " ";
        }
        cout << endl;
    }

    return 0;
}
